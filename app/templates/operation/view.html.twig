{% extends 'base.html.twig' %}

{% block title %}
    One Operation
{% endblock %}

{% block body %}
    <h1>
        Pojedyncza operacja
    </h1>
    {% if paginator.data|length %}
        <div>
            {{ operation.id }}. {{ operation.name }}
        </div>
    {% else %}
        <div>
            Brak kategorii!
        </div>
    {% endif %}
{% endblock %}